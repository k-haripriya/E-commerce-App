import { StyleSheet } from "react-native";

export const styles=StyleSheet.create({
    Container:{
        backgroundColor:'whitesmoke',
        borderRadius:30,
        padding:10,
    },
    textInput:{
        fontSize:16,
    }
})